# 短视频复刻项目 剩余开发工作清单

> 整理日期：2026-08-14
> 适用范围：短视频复刻工作台（仓库 1）+ 口播数字人 Skill（仓库 2）+ 相关 Skill 生态
> 用途：团队后续开发逐项勾销的待办主文档
> 原则：本清单只列出「未完成 / 待验证 / 待决策」事项；每项可回溯到来源文档。完成一项即勾选一项。

---

## 0. 总体状态

### 仓库 1：短视频复刻工作台

`/Users/honor.pei/.codex/.chatgpt-projects/g-p-6a729966aa0c81919204ec93cd55e8da`（分支 `codex/feat-windows-auto-bootstrap`，26 个提交）

- **V1.2 P0 本地可验证骨架已完成**：工程骨架、SQLite+WAL+备份恢复、登录/RBAC、设置与密钥、COS/OSS 存储、上传预检、Gemini 拆解、镜头卡片编辑、人物库、清晰帧候选、人物置换首帧、口播稿、Prompt、H3 Provider（Fake + 真实适配）、动态数量、批次编排、进度聚合、结果归档质检均有实现与测试。
- **Gate 1（本地 FakeProvider 全流程）**：✅ 2026-08-14 复核全绿 —— 服务端 153 项 pytest、前端测试、Tauri/Rust cargo fmt+check、Ruff、Mypy 全部通过。
- **Gate 2（真实 Provider）**：部分完成 —— METASO H3 创建/查询已真实验证（commit `8f64c56`），但**完整归档、取消/回调/失败状态全集仍 SOURCE_PENDING**；Apilio 三模型、Nano Banana 别名、Gemini json_schema、H3 原生音轨均未实测。
- **Gate 3（Windows 内测）**：打包配置就绪（Tauri NSIS、currentUser、禁止降级、icon 齐全、运维手册已建），**未做实机验证、签名未配置、10-20 项目试跑未开始**。
- 当前只能标记为 `LOCALLY_VERIFIED` 或 `UNSIGNED_INTERNAL_TEST`，不能标记 `PROVIDER_VERIFIED` / `READY_FOR_INTERNAL_RELEASE`。

### 仓库 2：口播数字人 Skill

`/Users/honor.pei/Documents/ChatGPT/口播数字人视频`（分支 `feat/minimax-h3-talking-avatar-skill`，9 个提交）

- `create-minimax-h3-talking-avatar` 已达 **V0.2 人工质检闸门基线**：人物图 + 文案 → 首帧 → 人工审批 → H3 I2V → MP4 的单段闭环可用。
- 遗留：Metaso 完整文档待补、自动模式未开放、多段拆分等能力明确不在当前版本。

---

## 一、P0 上线阻塞项（V1.2 P0 内部可发布前必须完成）

> 只有 Gate 1/2/3 全部通过，V1.2 P0 才能标记为内部可发布。

### 1. G0 外部 Provider 门禁（未解除）

> 来源：`docs/短视频复刻桌面端开发说明-技术评审.md` §5、`docs/短视频复刻桌面端开发说明.md` §11.7 / §16.3、`docs/真实Provider验收记录模板.md`
> 代码侧评估（2026-08-14）：创建/查询/归档/音轨检测/模型别名**均已实现**；剩余均为「真实账号验收」或「需外部接口说明」项。

| 门禁项 | 代码侧现状 | 待办性质 |
| --- | --- | --- |
| **METASO H3 取消 / 回调 / 失败状态全集说明** | 创建、查询、结果归档已实现（`MetasoH3Provider`，commit `8f64c56`）；`TERMINAL_STATUSES={FAILED,CANCELLED}`、`SUBMISSION_UNCERTAIN` 已映射；**取消接口无代码实现**，失败状态全集未完整覆盖 | 需 METASO 官方接口说明（取消/回调/失败全集）后才能补实现，禁止猜测 |
| **Apilio 三个模型名真实支持验证** | `APILIO_GEMINI_MODEL="gemini-3.1-pro-preview"`（analysis.py）、`gpt-image-2`/`nano-banana-pro-2k`（first_frames.py）已定义 | 需真实账号实测当前令牌组 |
| **Nano Banana Pro 2K 实际模型别名实测** | 代码别名 `nano-banana-pro-2k` | 需真实账号实测别名与 9:16/2K 能力 |
| **Gemini json_schema 支持实测** | 代码直接用 `response_format={"type":"json_object"}`（analysis.py:146/164），即已预先采用降级路径 | 需实测 json_schema 是否可用；可用则评估是否切换 |
| **H3 真实 I2V 结果含可播放原生音轨的证明** | `h3_audio_quality()`（generation.py:1384）用 ffprobe 检测 audio stream，FakeProvider 测试已覆盖 | 需真实 H3 结果 MP4 验证 |
| **已暴露 METASO 示例密钥吊销与轮换** | 无代码缺口 | 操作项，需用户执行（吊销 → 新密钥入服务端密钥存储） |

### 2. 真实 Provider 验收（D1-02 / Gate 2）

> 来源：`docs/真实Provider验收记录模板.md`（当前 6 项 Provider 结论全部为 SOURCE_PENDING）
> 前置评估：FakeProvider 契约/E2E 测试、模型别名、归档与音轨检测代码均已就绪；**唯一阻塞是真实付费账号与可复现凭证**。

- [ ] **Gemini 最小视频拆解** —— 真实 COS/OSS 签名 URL 端到端，脱敏样本归档。
- [ ] **GPT Image 2 真实生成一条**。
- [ ] **Nano Banana Pro 2K 真实生成一条**。
- [ ] **H3 创建 → 查询 → 下载 → 企业存储归档最小闭环**。
- [ ] **H3 原生音轨可播放验证**。
- [ ] **429 / 余额不足等可控错误** —— 错误映射与脱敏记录。
- [ ] **费用与失败率汇总** —— 填写验收模板结论；真实测试通过才可标记 `PROVIDER_VERIFIED`，未验证项保持 `SOURCE_PENDING`。

### 3. Windows 打包与内测（D2-01 / Gate 3）

> 来源：`docs/Windows内测与运维手册.md` §8（发布前检查清单全部「待填写」）
> 前置评估：NSIS 配置已就绪（`targets=["nsis"]`、`installMode=currentUser`、`allowDowngrades=false`、icon 齐全），`签名证书未配置`；**阻塞是 Windows 实机、签名证书与真实试跑项目**。本机为 macOS，无法在本机执行 x86_64-pc-windows-msvc 构建验证。

- [ ] **Windows x64 NSIS installer 实机生成与验证**。
- [ ] **签名证书配置** —— 当前未签名，只能按 `UNSIGNED_INTERNAL_TEST` 分发；签名后记录证书指纹、时间戳服务、SHA256 与验证截图。
- [ ] **普通员工安装 / 升级不丢 SQLite 数据 / 卸载策略验证**。
- [ ] **备份恢复演练 + 关闭桌面端后任务继续运行 + 重开进度恢复**。
- [ ] **10-20 个真实项目试跑**，汇总成本、失败率、质量问题。
- [ ] **确认日志无密钥、无完整签名 URL**。

### 4. 提交累积的 Not-tested 项（尚未验证）

> 来源：最近 7 条提交的 `Not-tested:` 标注

- [ ] 真实阿里 OSS 账号验收（`1c4180e`）。
- [ ] Windows 安装包人工界面验收（`1c4180e`）。
- [ ] Windows 打包后自动拉起 Worker（`8f64c56`）。
- [ ] 真实 COS/OSS 配置下，本地 Worker 调用真实 METASO 的完整归档验收（`8f64c56`）。
- [ ] 真实 COS/OSS 签名 URL 调用 Apilio Gemini 的端到端验收（`7e63990`）。
- [ ] Nano Banana、Gemini 视频 URL、人物替换语义与真实 H3 闭环（`92f4714`）。
- [ ] 真实 Apilio multipart、URL/b64 响应及真实 COS/OSS 归档（`97a8450`）。
- [ ] 真实 COS/OSS、Windows 打包环境及真人参考视频上验收评分阈值（`6407e77`）。

---

## 二、P1 阶段功能（已规划未开发，明确不进入当前范围）

> 来源：`docs/短视频复刻桌面端开发说明.md` §2.2
> P1 必须遵守 H3 输入互斥规则：`first_frame` / `last_frame` 与 `reference_image` / `reference_video` / `reference_audio` 不得混用；多对象参考是另一种请求模式，不是「带首帧的增强版图生视频」。

- [ ] **文生视频（T2V）**。
- [ ] **多对象参考（R2V）** —— reference_image / reference_video / reference_audio 的对象编排。
- [ ] **H3 Context IR 可选能力**。
- [ ] **声音克隆、TTS 和音画后期链路**（应使用独立模块和迁移，不在 P0 预埋声音字段）。
- [ ] **批量项目、模板和团队审核能力**。

---

## 三、口播数字人 Skill 遗留（create-minimax-h3-talking-avatar）

> 来源：`docs/口播数字人Skill开发说明.md`、`references/api-contract.md`
> 以下各项**等待外部资料或用户明确要求**，不得自行提前实现。

- [ ] **Metaso 完整 API 说明到达后，更新 `references/api-contract.md` 与测试**。
- [ ] **Metaso 代理查询路径、错误体、超时策略验证**（当前 api-contract.md §147-153 列为「Metaso 待验证项」）。
- [ ] **用户实际口播样片与人物一致性 Golden Sample**。
- [ ] **自动批准模式** —— 当前固定人工批准；只有用户未来明确要求启用自动模式，才能修改这一契约。

---

## 四、Skill 生态与工程收尾

- [x] **`video-reverse-prompt-script-firstframe/SKILL.md` 版本说明** —— 已核实 V0.6 是交付契约版本（SCHEMA_VERSION、分镜 JSON、Release Gate 校验），V0.6.4 是外层安装引导层版本，属分层设计、标题不改；已在 SKILL.md 标题下补版本说明消除歧义。86 项测试全绿。
- [x] **处理残留空壳目录** `口播数字人视频/.codex/skills/minimax-h3-video/` —— 已删除。该目录仅含空 `agents/`、`references/`，未被 git 跟踪（内容已于 commit `0c4a5e6` 迁入 `create-minimax-h3-talking-avatar`），属残留空壳，已安全清理。
- [ ] **当前分支 `codex/feat-windows-auto-bootstrap` 合并回 `main`** —— 26 个提交、2500+ 行变更仍停留在分支，`main` 仅 1 个基线提交。
- [ ] **（架构评审点）仓库 1 与仓库 2 的 H3 客户端功能重叠** —— `server/app/generation.py` 与 `h3_video.py` 共用同一 H3 语义，评估是否收敛为共享模块。

---

## 五、明确不进入当前范围（防 scope creep，不应开发）

> 来源：`docs/短视频复刻桌面端开发说明.md` §2.3、口播数字人 Skill 开发说明

- 逐镜头分别调用 H3 后自动拼接。
- 专业非线性时间线编辑器。
- 对外开放注册、支付、会员、积分商城。
- 自动发布到抖音、视频号等平台。
- 未经授权的人脸或声音克隆。
- 独立音频上传、声音库、Voice Clone、TTS、音轨替换、混音、口型同步。
- 承诺逐字对白、帧级口型同步或 100% 人物一致。
- 未经实测直接切换供应商模型别名。

---

## 附：来源索引

| 文档 | 作用 |
| --- | --- |
| `docs/短视频复刻桌面端-V1.2开发任务计划.md` | 唯一 roadmap（G0 → A/B/C → D 依赖图、三 Gate 发布门禁） |
| `docs/短视频复刻桌面端开发说明.md` | 唯一主任务书（P0 功能全集、§2.2 P1、§2.3 不进入范围、§11 Provider 规范、§16 状态机） |
| `docs/短视频复刻桌面端开发说明-技术评审.md` | §5 未解除外部门禁 |
| `docs/Windows内测与运维手册.md` | D2-01 交付边界、§8 发布前检查清单 |
| `docs/真实Provider验收记录模板.md` | Gate 2 验收记录与结论规则 |
| `video-reverse-prompt-script-firstframe/SKILL.md` | Skill 交付边界（双交付物） |
| `口播数字人视频/docs/口播数字人Skill开发说明.md`、`.../references/api-contract.md` | 口播数字人 Skill 契约与待补资料 |
